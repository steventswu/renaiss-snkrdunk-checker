{
  "name": "renaiss-index-companion",
  "private": true,
  "version": "2.4.0",
  "scripts": {
    "build": "esbuild src/popup.jsx --bundle --format=iife --target=chrome109 --outfile=popup.bundle.js",
    "check": "node --check content.js && node --check background.js && node --check page-bridge.js && node --check popup.bundle.js"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.24.2"
  }
}
